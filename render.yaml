services:
  - type: web
    name: officemessenger
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class gthread --workers 1 --threads 8 --bind 0.0.0.0:$PORT server:app
